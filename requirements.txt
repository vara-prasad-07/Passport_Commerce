# Core web framework — one FastAPI app per service (passport, marginmind, buyer-agent)
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Passport signing/verification (Ed25519)
cryptography==50.0.1

# HTTP between services (buyer-agent -> passport, buyer-agent -> marginmind)
httpx==0.28.1

# .env loading
python-dotenv==1.2.3

# LLM + agent framework (buyer-agent: intent parsing, negotiation, explanations)
groq==0.37.1
langchain==1.4.0
langchain-groq==1.1.3
langchain-core==1.6.1

# Razorpay test-mode payments (falls back to a simulated flow if no keys are set)
razorpay==2.0.1

# Tests
pytest==9.1.1
